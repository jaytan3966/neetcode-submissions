class Solution:
    def predictPartyVictory(self, senate: str) -> str:
        inn = deque([])
        out = deque([])

        n = len(senate)

        return "Radiant"
        
        # for i in range(n):
        #     if inn
